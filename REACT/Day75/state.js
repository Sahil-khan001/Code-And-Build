const state = {
    slice1 : {
        count : 0,
    },

    slice2 ={
        count : 2,
    },
}


//so basically useSelector gives us GlobalState which contains all the state acc to their slices that's why we use state.slice1.count in the useSelector 